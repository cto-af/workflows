name: Publish Package to npmjs
on:
  workflow_call:
    inputs:
      docs:
        description: 'Publish docs?'
        required: false
        type: boolean
        default: false
      script:
        description: 'Which npm script to run'
        required: false
        type: string
        default: build
      workspaces:
        description: 'Add --workspaces to the publish step'
        required: false
        type: boolean
        default: false
jobs:
  build:
    runs-on: ubuntu-latest
    environment: publish
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: 'https://registry.npmjs.org'
      - uses: pnpm/action-setup@v5
        with:
          cache: true
          run_install: true
      - name: Running ${{ inputs.script }} on node ${{ matrix.node-version }}, ${{ matrix.platform }}
        run: npm run ${{ inputs.script }}
      - run: pnpm audit
      - name: Deploy Docs
        if: inputs.docs
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: docs
          publish_branch: gh-pages
      - run: npm pkg delete devDependencies scripts ${{ (inputs.workspaces && '--workspaces') || '' }}
      - run: npm publish --access public --provenance ${{ (inputs.workspaces && '--workspaces') || '' }}
